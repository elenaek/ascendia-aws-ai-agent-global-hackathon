# ============================================================================
# Root Requirements - Deployment Scripts
# ============================================================================
# Python packages required for running deployment scripts:
#   - scripts/deploy-all.sh (orchestrator)
#   - backend/scripts/deploy-agentcore.py (AgentCore deployment)
#   - backend/scripts/attach-iam-policy.py (IAM policy attachment)
#   - scripts/check-bedrock-models.py (model access verification)
#   - infrastructure/ (CDK deployment)
#
# Install with: pip install -r requirements.txt
# ============================================================================

# ----------------------------------------------------------------------------
# AWS SDK - Core AWS API interactions
# ----------------------------------------------------------------------------
boto3>=1.40.48
botocore>=1.40.48

# ----------------------------------------------------------------------------
# AWS Bedrock AgentCore - AI Agent runtime and primitives
# ----------------------------------------------------------------------------
# Required by backend/scripts/deploy-agentcore.py for:
#   - Memory management (conversation history storage)
#   - Identity management (API key credential providers)
#   - Agent deployment and configuration
bedrock-agentcore>=0.1.7
bedrock-agentcore-starter-toolkit>=0.1.21

# ----------------------------------------------------------------------------
# AWS CDK - Infrastructure as Code
# ----------------------------------------------------------------------------
# Required by infrastructure/ for deploying:
#   - Cognito (authentication)
#   - DynamoDB (data storage)
#   - Lambda (serverless functions)
#   - WebSocket API (real-time communication)
aws-cdk-lib==2.215.0
constructs>=10.0.0,<11.0.0

# ----------------------------------------------------------------------------
# Configuration & Utilities
# ----------------------------------------------------------------------------
# python-dotenv: Load environment variables from .env files
# pyyaml: Parse AgentCore YAML configuration files
python-dotenv>=1.1.1
pyyaml>=6.0.3

# ============================================================================
# Additional Notes
# ============================================================================
#
# For full backend development (including the agent runtime), see:
#   backend/requirements.txt
#
# For CDK development only, see:
#   infrastructure/requirements.txt
#
# This file contains only the minimal dependencies needed to run the
# deployment scripts from the root directory.
# ============================================================================
